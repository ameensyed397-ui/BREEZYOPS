---
tags: [memory]
---
# Changelog

- **v0.1** — Initial vault: full PRD, architecture (stack/system/data/RBAC/integrations/deploy), 13 feature specs with flows/UI states/use+edge cases, 6 userflows, 7 SOPs, contract/invoice/quote/comms templates, 3-phase roadmap, decision log, gaps analysis, project memory.
- **v0.2** (2026-07-13) — First real code: consolidated two divergent scaffolds into one `breezyops/` app, fixed a login flow that could never complete (missing OTP verify step), wired real Supabase auth (email + phone OTP), built F02 Lead Pipeline (B2C + B2B kanban, drag-to-move-stage). Added `deals` table + RLS policies. Git history started (previously uncommitted). Full detail in [[Build-Log]]; live status in [[Build-Status]].
- **v0.3** (2026-07-13) — Built F03 Appointments & Scheduling (day/week calendar, booking sheet with a real double-booking conflict guard). User started running a second AI tool (opencode) concurrently in the same working tree, which built F04 Jobs, F05 Customers, and F09 Invoicing (list+detail UI, mock data) in parallel. Verified the full combined app builds clean before committing. Closed an RLS gap on the new `deals` table and an audit-log spoofing gap found via self-audit.
- **v0.4** (2026-07-14) — All pages wired to real Supabase DB (via HTTPS client, direct PG blocked by IPv6). 13 query functions + 5 server actions with revalidation. Staging hardening: fixed broken auth confirm flow, non-functional webhook, hardcoded sidebar badge, open redirect vulnerability; added env validation, middleware error handling, sanitized query errors, security headers, root error/404 pages. App builds clean, staging-ready.
- **v0.5** (2026-07-14) — All client mutations wired to server actions (lead qualify/book/lose, deal stage drag-drop, appointment booking, job status, invoice pay/void). Settings page fully wired to real DB (profiles, service catalog with inline price editing, localities with add, audit log). Rate limiting added (in-memory sliding-window: auth 10/min, webhook 30/min). Vitest installed with 21 tests. Fixed protocol-relative URL bypass in auth redirect validation. Build passes clean, zero warnings.
- **v0.6** (2026-07-14) — RLS policies applied to live Supabase project. 20 RBAC policies across 13 tables, helper functions, auto-profile trigger, catalog_public view. All Phase 1 infrastructure work complete.
- **v0.7** (2026-07-14) — Full UX sweep. Mobile navigation (hamburger drawer), dark mode (next-themes), active route highlighting. Login: form labels, input types, resend OTP, auto-focus, rate limit feedback. Loading skeletons for all routes. Dashboard: dynamic greeting, clickable KPIs, refresh. Leads: search, SLA countdown. Pipeline: keyboard DnD, aria-live, error toasts. Schedule: week view responsive, day view time indicator. Invoices: horizontal scroll. Settings: invite user message. Shared InfoCard component extracted. 37 UX issues resolved.
- **v0.8** (2026-07-14) — LOW polish sweep + React 19 strict mode. Removed all `as any` type casts (21 files). Fixed `setTab` typing across 5 list components. Applied ScrollArea to 4 detail sheets, shadcn Table to settings catalog. Fixed channelIcon semantics, statusLabel consistency, startOfWeek Monday. Pipeline info buttons. ESLint configured (eslint.config.mjs). React 19 compliance: lazy useState for localStorage, optimistic state pattern (no prop→state effects), extracted BookingSheetForm (no form-reset effect), Lucide Image→Icon rename. 16 routes, zero build errors, 21 tests passing. Production-ready.
- **v0.9** (2026-07-16) — Production hardening. Wrapped all 6 unprotected mutation functions in try-catch with mock fallbacks (prevents 500s without Supabase). Added error handling to all 10 server actions. Error boundaries now log instead of silently swallowing. Document library switched to card grid with gradient thumbnails. Invoice preview added (PDF in-dialog iframe). Pipeline kanban gets Notion-style drag handle and click-to-open detail sheets. Month-view calendar added. Tab selected state gets subtle drop shadow. BREEZYAIR branding on all invoice PDFs. Sheet padding standardized. Removed dead code, unused imports, console.warn cleanup. 16 routes, zero TS errors, zero lint warnings, 21 tests passing.
- **v0.10** (2026-07-16) — Day view rewrite + shadcn sidebar + consistency sweep. Day view: Notion-style column layout with hour grid, overlap detection, left accent border. Shadcn sidebar: collapsible icon mode, active nav highlighting (primary accent + left border), Caveat font brand, toggle in header. Dropdown overlap fix: added missing `--popover` CSS token, hover effects on SelectItem. Badge consistency: scheduled→outline, cancelled→destructive, lost stage added across pipeline cards + customer detail sheets. Visual hierarchy: deal values, revenue, totals upgraded to font-semibold. Dead header search removed, Search icon added to jobs. New customer booking flow: redirect to /customers with returnTo param. Week view today highlight: bg-primary/5. Layout spacing normalized across all 9 pages (mx-auto max-w-6xl px-6 py-8). Caveat font for Breezyops brand. Logo visibility improved. 16 routes, zero TS errors, zero lint warnings, 21 tests passing. Not committed — awaiting user review.
